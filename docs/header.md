# Fiap TC-1 API

API REST para gerenciamento de ordens de serviço de mecânica.

## Autenticação

Todos os endpoints protegidos requerem o header `Authorization` com um token Bearer:

```
Authorization: Bearer <token>
```
